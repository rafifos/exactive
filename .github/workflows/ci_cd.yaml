name: CI/CD

on:
  push:
    branches: master

jobs:
  prepare-environment:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Install
        uses: borales/actions-yarn@v2.0.0
        with:
          cmd: install

  lint:
    runs-on: ubuntu-latest

    needs: prepare-environment

    - name: Lint
      uses: borales/actions-yarn@v2.0.0
      with:
          cmd: build

  test:
    runs-on: ubuntu-latest

    needs: prepare-environment

    - name: Unit tests
      uses: borales/actions-yarn@v2.0.0
      with:
        cmd: test --passWithNoTests

    - name: E2E tests
      uses: borales/actions-yarn@v2.0.0
      with:
        cmd: test:e2e --passWithNoTests

    - name: Coverage tests
      uses: borales/actions-yarn@v2.0.0
      with:
        cmd: test:cov --passWithNoTests

  build:
    runs-on: ubuntu-latest

    needs: prepare-environment

    - name: Build
      uses: borales/actions-yarn@v2.0.0
      with:
        cmd: build

  release:
    runs-on: ubuntu-latest

    needs: [prepare-environment, build]

    - name: Release
      uses: borales/actions-yarn@v2.0.0
      with:
        cmd: semantic-release